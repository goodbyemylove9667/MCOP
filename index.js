/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Run from 'SystemManager/Navigation/Export';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Run);
